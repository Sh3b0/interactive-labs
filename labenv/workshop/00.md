# Empty Workshop

Workshop files are expected in `/app/workshop` in the `labenv` container or VM.

> Try loading the [default workshop](https://github.com/Sh3b0/interactive-labs/tree/main/workshop) using the button above.
