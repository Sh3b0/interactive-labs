name: Build VM Images

on:
  workflow_dispatch:
    inputs:
      provider:
        description: 'Provider to build'
        required: true
        default: 'qemu'
        type: choice
        options:
        - qemu
        - virtualbox
      arch:
        description: 'Architecture'
        required: true
        default: 'amd64'
        type: choice
        options:
        - amd64
        - arm64

permissions:
  contents: write

jobs:
  build:
    name: Build VM Images
    runs-on: ${{ inputs.arch == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-utils wget

      - name: Install d2vm
        run: |
          wget https://github.com/linka-cloud/d2vm/releases/download/v0.4.0/d2vm_v0.4.0_linux_${{ inputs.arch }}.tar.gz
          tar -C /usr/local/bin -xzvf d2vm_v0.4.0_linux_${{ inputs.arch }}.tar.gz d2vm

      - name: Build Docker Image
        uses: docker/build-push-action@v5
        with:
          context: vms/build
          file: vms/build/Dockerfile
          platforms: linux/${{ inputs.arch }}
          tags: labenv:vm-${{ inputs.arch }}
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Create QCOW2 Disk
        working-directory: vms/build
        run: |
          BOOTLOADER="grub"
          if [ "${{ inputs.arch }}" = "arm64" ]; then
            BOOTLOADER="grub-efi"
          fi
          
          sudo d2vm convert \
            --platform linux/${{ inputs.arch }} \
            --bootloader $BOOTLOADER \
            --network-manager netplan \
            --size 20G \
            --password vagrant \
            --output labenv-${{ inputs.arch }}.qcow2 \
            labenv:vm-${{ inputs.arch }}

      - name: Build VirtualBox Box
        if: inputs.provider == 'virtualbox' && inputs.arch == 'amd64'
        working-directory: vms/build/virtualbox
        run: |
          qemu-img convert -O vmdk ../labenv-amd64.qcow2 box.vmdk
          tar czfv labenv-virtualbox-amd64.box ./*

      - name: Upload to Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: boxes
          files: |
            vms/build/**/*.qcow2
            vms/build/**/*.box
