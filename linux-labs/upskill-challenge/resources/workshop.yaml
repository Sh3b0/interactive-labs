apiVersion: training.educates.dev/v1beta1
kind: Workshop
metadata:
  name: "upskill-challenge"
spec:
  title: "Linux Upskill Challenge"
  description: "Labs from linuxupskillchallenge.org"
  publish:
    image: "$(image_repository)/upskill-challenge-files:$(workshop_version)"
  workshop:
    files:
    - image:
        url: "$(image_repository)/upskill-challenge-files:$(workshop_version)"
      includePaths:
      - /workshop/**
      - /exercises/**
      - /README.md
  session:
    namespaces:
      budget: medium
    applications:
      examiner:
        enabled: true
      terminal:
        enabled: true
      editor:
        enabled: false
      console:
        enabled: false
      docker:
        enabled: false
      registry:
        enabled: false
      vcluster:
        enabled: false

